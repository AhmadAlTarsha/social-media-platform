version: '3.8'

services:
  app:
    build: 
      context: .
      dockerfile: dockerfile
    container_name: social-media-platform
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules